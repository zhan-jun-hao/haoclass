设计表的时候，多想一下整个流程，谁要干什么，事件完成的标志，开始事件的人、加工、结果、过程

# 微服务1：main-service

# 1.课程表

```sql
CREATE TABLE `course` (
  `id` bigint unsigned NOT NULL COMMENT '课程ID，建议使用 MyBatis-Plus ASSIGN_ID 雪花ID',
  `categoryName` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '课程分类名称，例如：JavaSE、JavaEE、Redis、MySQL',
  `title` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '课程标题',
  `subtitle` varchar(200) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '课程副标题/卖点文案',
  `coverUrl` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '课程封面图URL',
  `teacherName` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '讲师名称，轻量版可先冗余保存',
  `summary` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '课程摘要，用于列表和详情页简短介绍',
  `detail` text COLLATE utf8mb4_unicode_ci COMMENT '课程详情，可存富文本HTML或Markdown',
  `price` int unsigned NOT NULL DEFAULT '0' COMMENT '课程售价，单位：分',
  `episodeCount` int unsigned NOT NULL DEFAULT '0' COMMENT '课程章节数，由章节表同步维护',
  `buyCount` int unsigned NOT NULL DEFAULT '0' COMMENT '购买人数/销量',
  `sort` int NOT NULL DEFAULT '0' COMMENT '排序值，越大越靠前',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '课程状态：0草稿 1上架 2下架',
  `chargeType` tinyint NOT NULL DEFAULT '1' COMMENT '收费类型：0免费 1付费 2VIP免费',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  KEY `idx_client_list` (`deleted`,`status`,`categoryName`,`sort`),
  KEY `idx_admin_list` (`deleted`,`status`,`createTime`),
  KEY `idx_title` (`title`),
  KEY `idx_createTime` (`createTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程表';
```

# 2.用户表

```sql
CREATE TABLE `user` (
  `id` bigint unsigned NOT NULL COMMENT '用户ID',
  `phone` varchar(20) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '手机号',
  `nickname` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '昵称',
  `avatarUrl` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '头像URL',
  `password` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '密码密文',
  `role` tinyint NOT NULL DEFAULT '0' COMMENT '角色：0普通用户 1管理员',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态：0禁用 1正常',
  `vipExpireTime` datetime DEFAULT NULL COMMENT 'VIP到期时间',
  `lastLoginTime` datetime DEFAULT NULL COMMENT '最后登录时间',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_admin_list` (`deleted`,`status`,`createTime`),
  KEY `idx_nickname` (`nickname`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

# 3.课程章节表

```sql
CREATE TABLE `course_episode` (
  `id` bigint unsigned NOT NULL COMMENT '章节ID',
  `courseId` bigint unsigned NOT NULL COMMENT '课程ID',
  `title` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '章节标题',
  `videoUrl` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '视频地址',
  `durationSeconds` int unsigned NOT NULL DEFAULT '0' COMMENT '视频时长，单位：秒',
  `freePreview` tinyint NOT NULL DEFAULT '0' COMMENT '是否试看：0否 1是',
  `sort` int NOT NULL DEFAULT '0' COMMENT '排序值，越小越靠前',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '状态：0草稿 1上架 2下架',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  KEY `idx_course_admin` (`courseId`,`deleted`,`sort`),
  KEY `idx_course_client` (`courseId`,`deleted`,`status`,`sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程章节表';
```

# 4.课程订单表

`course_order中 订单号、课程标题快照、课程封面快照、第三方支付流水号是什么，我第一次见`

快照：存一份当时购买的课程的标题和封面，如果说后面要改，就显示以前的快照

订单号：id主键是给程序看的，订单号是给外部实体看的，我们可以根据可读性更高的订单号来对账

第三方支付流水号：微信支付、支付宝支付成功后返回的支付平台流水号

```sql
CREATE TABLE `course_order` (
  `id` bigint unsigned NOT NULL COMMENT '订单ID，雪花ID',
  `orderNo` varchar(64) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '订单号，对外展示和支付使用',
  `userId` bigint unsigned NOT NULL COMMENT '用户ID',
  `courseId` bigint unsigned NOT NULL COMMENT '课程ID',
  `userCouponId` bigint unsigned DEFAULT NULL COMMENT '使用的用户优惠券ID',
  `couponName` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '优惠券名称快照',
  `discountAmount` int unsigned NOT NULL DEFAULT '0' COMMENT '优惠金额，单位：分',
  `courseTitle` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '课程标题快照',
  `coverUrl` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '课程封面快照',
  `coursePrice` int unsigned NOT NULL DEFAULT '0' COMMENT '课程原价，单位分',
  `payAmount` int unsigned NOT NULL DEFAULT '0' COMMENT '实付金额，单位分',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '订单状态：0待支付 1已支付 2已取消 3已退款 4已关闭',
  `payType` tinyint NOT NULL DEFAULT '0' COMMENT '支付方式：0未支付 1微信 2支付宝 3模拟支付',
  `thirdTradeNo` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '第三方支付流水号',
  `expireTime` datetime DEFAULT NULL COMMENT '订单过期时间',
  `payTime` datetime DEFAULT NULL COMMENT '支付时间',
  `cancelTime` datetime DEFAULT NULL COMMENT '取消时间',
  `refundTime` datetime DEFAULT NULL COMMENT '退款时间',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_orderNo` (`orderNo`),
  KEY `idx_user_list` (`userId`,`deleted`,`status`,`createTime`),
  KEY `idx_admin_list` (`deleted`,`status`,`createTime`),
  KEY `idx_course_user` (`courseId`,`userId`,`deleted`),
  KEY `idx_expire_scan` (`status`,`expireTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程订单表';
```

# 5.课程用户权益表

```sql
CREATE TABLE `course_user` (
  `id` bigint unsigned NOT NULL COMMENT '用户课程权益ID，雪花ID',
  `userId` bigint unsigned NOT NULL COMMENT '用户ID',
  `courseId` bigint unsigned NOT NULL COMMENT '课程ID',
  `orderId` bigint unsigned NOT NULL DEFAULT '0' COMMENT '来源订单ID',
  `sourceType` tinyint NOT NULL DEFAULT '0' COMMENT '来源：0购买 1后台赠送 2免费领取',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '权益状态：0失效 1有效 2退款失效',
  `grantTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '获得权益时间',
  `expireTime` datetime DEFAULT NULL COMMENT '权益过期时间，NULL表示永久有效',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_course` (`userId`,`courseId`),
  KEY `idx_user_courses` (`userId`,`status`,`deleted`,`createTime`),
  KEY `idx_course_users` (`courseId`,`status`,`deleted`),
  KEY `idx_orderId` (`orderId`),
  KEY `idx_expire_scan` (`status`,`expireTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户课程权益表';
```

# 6.课程学习进度表

```sql
CREATE TABLE `course_learning_progress` (
  `id` bigint unsigned NOT NULL COMMENT '课程学习进度ID，雪花ID',
  `userId` bigint unsigned NOT NULL COMMENT '用户ID',
  `courseId` bigint unsigned NOT NULL COMMENT '课程ID',
  `episodeId` bigint unsigned NOT NULL COMMENT '章节ID',
  `progressSeconds` int unsigned NOT NULL DEFAULT '0' COMMENT '最后播放进度，单位：秒',
  `finished` tinyint NOT NULL DEFAULT '0' COMMENT '是否学完：0未学完 1已学完',
  `finishTime` datetime DEFAULT NULL COMMENT '完成时间',
  `lastLearnTime` datetime DEFAULT NULL COMMENT '最后学习时间',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_course_episode` (`userId`,`courseId`,`episodeId`),
  KEY `idx_user_course` (`userId`,`courseId`,`deleted`),
  KEY `idx_user_last_learn` (`userId`,`deleted`,`lastLearnTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户课程学习进度表';
```

# 7.用户评论表

```sql
CREATE TABLE `course_comment` (
  `id` bigint unsigned NOT NULL COMMENT '评论ID，雪花ID',
  `courseId` bigint unsigned NOT NULL COMMENT '课程ID',
  `episodeId` bigint unsigned NOT NULL COMMENT '章节ID',
  `userId` bigint unsigned NOT NULL COMMENT '评论用户ID',
  `parentId` bigint unsigned NOT NULL DEFAULT '0' COMMENT '直接父评论ID，0表示一级评论',
  `rootId` bigint unsigned NOT NULL DEFAULT '0' COMMENT '根评论ID，0表示一级评论',
  `content` varchar(1000) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '评论内容',
  `likeCount` int unsigned NOT NULL DEFAULT '0' COMMENT '点赞数',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '评论状态：0待审核 1已发布 2隐藏 3已驳回',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  KEY `idx_episode_root_list` (`courseId`,`episodeId`,`rootId`,`deleted`,`status`,`createTime`),
  KEY `idx_root_reply` (`rootId`,`deleted`,`status`,`createTime`),
  KEY `idx_admin_list` (`deleted`,`status`,`createTime`),
  KEY `idx_user_list` (`userId`,`deleted`,`createTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程章节评论表';
```

# 8.用户点赞表

```sql
CREATE TABLE `course_comment_like` (
  `id` bigint unsigned NOT NULL COMMENT '评论点赞记录ID，雪花ID',
  `commentId` bigint unsigned NOT NULL COMMENT '被点赞评论ID',
  `userId` bigint unsigned NOT NULL COMMENT '点赞用户ID',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '点赞状态：0已取消 1已点赞',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_comment` (`userId`,`commentId`),
  KEY `idx_comment_status` (`commentId`,`status`),
  KEY `idx_user_status` (`userId`,`status`,`updateTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='课程评论点赞表';
```

# 微服务2：coupon-service

# 9.优惠券记录表

```sql
CREATE TABLE `coupon_template` (
  `id` bigint unsigned NOT NULL COMMENT '优惠券模板ID，雪花ID',
  `couponName` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '优惠券名称',
  `description` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '优惠券说明',
  `thresholdAmount` int unsigned NOT NULL DEFAULT '0' COMMENT '使用门槛金额，单位：分，0表示无门槛',
  `discountAmount` int unsigned NOT NULL COMMENT '优惠金额，单位：分',
  `totalStock` int unsigned NOT NULL COMMENT '发行总数量',
  `receivedCount` int unsigned NOT NULL DEFAULT '0' COMMENT '已领取数量',
  `receiveStartTime` datetime NOT NULL COMMENT '领取开始时间',
  `receiveEndTime` datetime NOT NULL COMMENT '领取结束时间',
  `validStartTime` datetime NOT NULL COMMENT '使用有效期开始时间',
  `validEndTime` datetime NOT NULL COMMENT '使用有效期结束时间',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '优惠券状态：0草稿 1已发布 2已停止',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  KEY `idx_client_receive` (`deleted`,`status`,`receiveStartTime`,`receiveEndTime`),
  KEY `idx_admin_list` (`deleted`,`status`,`createTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='优惠券模板表';
```



# 10.用户领取优惠券表

**用户已经选择这张优惠券创建了待支付订单，但还没有完成付款。**

```sql
CREATE TABLE `user_coupon` (
  `id` bigint unsigned NOT NULL COMMENT '用户优惠券ID，雪花ID',
  `couponTemplateId` bigint unsigned NOT NULL COMMENT '优惠券模板ID',
  `userId` bigint unsigned NOT NULL COMMENT '领取用户ID',
  `couponName` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '优惠券名称快照',
  `thresholdAmount` int unsigned NOT NULL DEFAULT '0' COMMENT '使用门槛金额快照，单位：分',
  `discountAmount` int unsigned NOT NULL COMMENT '优惠金额快照，单位：分',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '使用状态：0未使用 1已锁定 2已使用 3已过期',
  `orderId` bigint unsigned DEFAULT NULL COMMENT '锁定或使用该优惠券的订单ID',
  `lockTime` datetime DEFAULT NULL COMMENT '优惠券锁定时间',
  `lockExpireTime` datetime DEFAULT NULL COMMENT '锁定过期时间',
  `useTime` datetime DEFAULT NULL COMMENT '实际使用时间',
  `validStartTime` datetime NOT NULL COMMENT '有效期开始时间',
  `validEndTime` datetime NOT NULL COMMENT '有效期结束时间',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '领取时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_coupon_template` (`userId`,`couponTemplateId`),
  KEY `idx_user_status_create` (`userId`,`deleted`,`status`,`createTime`),
  KEY `idx_user_coupon_list` (`userId`,`deleted`,`status`,`validEndTime`),
  KEY `idx_orderId` (`orderId`),
  KEY `idx_locked_expire` (`status`,`lockExpireTime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

# 微服务3：pay-service

# 11.订单支付表

```sql
CREATE TABLE `payment_order` (
	`id` BIGINT UNSIGNED NOT NULL COMMENT '支付单ID，雪花ID',
	`paymentNo` VARCHAR ( 64 ) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '支付单号，由支付服务生成并提交给第三方支付平台',
	`bizType` TINYINT NOT NULL COMMENT '业务类型：1课程订单,暂时只有课程订单',
	`bizOrderId` BIGINT UNSIGNED NOT NULL COMMENT '业务订单ID，例如course_order.id',
	`bizOrderNo` VARCHAR ( 64 ) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '业务订单号快照，例如course_order.orderNo',
	`userId` BIGINT UNSIGNED NOT NULL COMMENT '支付用户ID',
	`subject` VARCHAR ( 200 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '支付标题，例如：购买Java课程',
	`payAmount` INT UNSIGNED NOT NULL COMMENT '支付金额，单位：分',
	`currency` VARCHAR ( 10 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'CNY' COMMENT '货币类型',
	`payChannel` TINYINT NOT NULL COMMENT '支付渠道：1微信 2支付宝 3模拟支付',
	`status` TINYINT NOT NULL DEFAULT '0' COMMENT '支付状态：0待支付 1支付成功 2已关闭 3支付失败',
	`thirdTradeNo` VARCHAR ( 100 ) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '第三方支付流水号',
    `codeUrl` VARCHAR(500) NOT NULL DEFAULT '' COMMENT '微信Native支付二维码内容',
	`failureReason` VARCHAR ( 255 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '支付失败原因',
	`expireTime` DATETIME NOT NULL COMMENT '支付单过期时间',
	`payTime` DATETIME DEFAULT NULL COMMENT '支付成功时间',
	`closeTime` DATETIME DEFAULT NULL COMMENT '支付单关闭时间',
	`createdUser` BIGINT UNSIGNED NOT NULL DEFAULT '0' COMMENT '创建人ID',
	`updatedUser` BIGINT UNSIGNED NOT NULL DEFAULT '0' COMMENT '更新人ID',
	`createTime` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
	`updateTime` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
	`deleted` TINYINT NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
	PRIMARY KEY ( `id` ),
	UNIQUE KEY `uk_paymentNo` ( `paymentNo` ),
	UNIQUE KEY `uk_biz_order` ( `bizType`, `bizOrderId` ),
	UNIQUE KEY `uk_channel_thirdTradeNo` ( `payChannel`, `thirdTradeNo` ),
	KEY `idx_user_list` ( `userId`, `status`, `createTime` ),
	KEY `idx_expire_scan` ( `status`, `expireTime` )
) ENGINE = INNODB DEFAULT CHARSET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '支付订单表';
```

# 12.mq本地消息表

## 12.1 为什么要建本地消息表？

因为发送消息和业务操作不是同一个事务，为了保证业务操作和发送消息在同一个事务里面，将业务表和本地消息表放在同一个数据库里面，做到事务一致

```sql
CREATE TABLE `mq_message` (
  `id` bigint unsigned NOT NULL COMMENT '消息ID，雪花ID',
  `bizType` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '业务类型，例如：PAYMENT_SUCCESS',
  `bizId` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '业务ID，例如：paymentNo',
  `exchangeName` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '交换机名称',
  `routingKey` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '路由键',
  `messageBody` text COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '消息体JSON',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '状态：0待发送 1已发送 2发送失败',
  `retryCount` int unsigned NOT NULL DEFAULT '0' COMMENT '重试次数',
  `nextRetryTime` datetime NOT NULL COMMENT '下次重试时间',
  `lastError` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '最后一次错误信息',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_biz_message` (`bizType`,`bizId`),
  KEY `idx_retry` (`deleted`,`status`,`nextRetryTime`),
  KEY `idx_biz_id` (`bizId`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='MQ本地消息表';
```

# 13.mq死信信息表

```sql
CREATE TABLE `mq_dead_message` (
  `id` bigint unsigned NOT NULL COMMENT '死信消息ID，雪花ID',
  `bizType` varchar(50) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '业务类型，例如：PAYMENT_SUCCESS',
  `bizId` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '业务ID，例如：paymentNo',
  `sourceQueue` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '原始业务队列名称',
  `sourceExchange` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '原始业务交换机名称',
  `sourceRoutingKey` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '原始业务路由键',
  `deadQueue` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '死信队列名称',
  `deadExchange` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '死信交换机名称',
  `deadRoutingKey` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '死信路由键',
  `messageBody` text COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '消息体JSON',
  `headers` text COLLATE utf8mb4_unicode_ci COMMENT '消息头JSON',
  `deadReason` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '进入死信原因',
  `lastError` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '最后一次处理错误',
  `status` tinyint NOT NULL DEFAULT '0' COMMENT '状态：0待处理 1已重投 2忽略 3重投失败',
  `retryCount` int unsigned NOT NULL DEFAULT '0' COMMENT '人工重投次数',
  `lastRetryTime` datetime DEFAULT NULL COMMENT '最后一次人工重投时间',
  `createdUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '创建人ID',
  `updatedUser` bigint unsigned NOT NULL DEFAULT '0' COMMENT '更新人ID',
  `createTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updateTime` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` tinyint NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
  PRIMARY KEY (`id`),
  KEY `idx_status_createTime` (`deleted`,`status`,`createTime`),
  KEY `idx_biz` (`bizType`,`bizId`),
  KEY `idx_source_queue` (`sourceQueue`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='MQ死信消息表';
```

# 14.订单退款表

```sql
CREATE TABLE `payment_refund_order` (
	`id` BIGINT UNSIGNED NOT NULL COMMENT '退款单ID，雪花ID',
	`refundNo` VARCHAR ( 64 ) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '退款单号，由支付服务生成并提交给第三方支付平台',
	`paymentNo` VARCHAR ( 64 ) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '原支付单号',
	`bizType` TINYINT NOT NULL COMMENT '业务类型：1课程订单',
	`bizOrderId` BIGINT UNSIGNED NOT NULL COMMENT '业务订单ID，例如course_order.id',
	`bizOrderNo` VARCHAR ( 64 ) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '业务订单号快照，例如course_order.orderNo',
	`userId` BIGINT UNSIGNED NOT NULL COMMENT '退款用户ID',
	`payAmount` INT UNSIGNED NOT NULL COMMENT '原支付金额，单位：分',
	`refundAmount` INT UNSIGNED NOT NULL COMMENT '退款金额，单位：分',
	`currency` VARCHAR ( 10 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'CNY' COMMENT '货币类型',
	`payChannel` TINYINT NOT NULL COMMENT '支付渠道：1微信 2支付宝 3模拟支付',
	`status` TINYINT NOT NULL DEFAULT '0' COMMENT '退款状态：0待退款 1退款中 2退款成功 3退款失败 4已关闭',
	`thirdTradeNo` VARCHAR ( 100 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '原第三方支付流水号',
	`thirdRefundNo` VARCHAR ( 100 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '第三方退款流水号',
	`refundReason` VARCHAR ( 255 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '退款原因',
	`failureReason` VARCHAR ( 500 ) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '' COMMENT '退款失败原因',
	`applyTime` DATETIME NOT NULL COMMENT '退款申请时间',
	`refundTime` DATETIME DEFAULT NULL COMMENT '退款成功时间',
	`closeTime` DATETIME DEFAULT NULL COMMENT '退款关闭时间',
	`createdUser` BIGINT UNSIGNED NOT NULL DEFAULT '0' COMMENT '创建人ID',
	`updatedUser` BIGINT UNSIGNED NOT NULL DEFAULT '0' COMMENT '更新人ID',
	`createTime` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
	`updateTime` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
	`deleted` TINYINT NOT NULL DEFAULT '0' COMMENT '逻辑删除：0未删除 1已删除',
	PRIMARY KEY ( `id` ),
	UNIQUE KEY `uk_refundNo` ( `refundNo` ),
	UNIQUE KEY `uk_biz_order` ( `bizType`, `bizOrderId` ),
	KEY `idx_paymentNo` ( `paymentNo` ),
	KEY `idx_user_list` ( `userId`, `status`, `createTime` ),
	KEY `idx_channel_thirdRefundNo` ( `payChannel`, `thirdRefundNo` ),
KEY `idx_createTime` ( `createTime` ) 
) ENGINE = INNODB DEFAULT CHARSET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '支付退款订单表';
```

